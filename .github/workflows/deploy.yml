name: Trigger Jenkins Deployment

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [closed]

jobs:
  trigger-jenkins:
    if: github.event.pull_request.merged == true || github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Trigger Jenkins Job
        run: |
          echo "🚀 Merge to main branch detected!"
          echo "This would trigger Jenkins pipeline for:"
          echo "- Docker image build"
          echo "- Push to Docker Hub"
          echo "- Email notification to admin"
          echo "Jenkins URL: ${{ secrets.JENKINS_URL }}"
          # Actual Jenkins trigger would use:
          # curl -X POST "${{ secrets.JENKINS_URL }}/job/mlops-assignment-1/build" \
          #   --user "${{ secrets.JENKINS_USER }}:${{ secrets.JENKINS_TOKEN }}"
          
      - name: Deploy Status
        run: |
          echo "✅ Deployment pipeline triggered successfully"
          echo "📧 Admin will receive email notification upon completion"
